{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Dart",
        "request": "launch",
        "type": "dart",
        "program": "E:/Assignment 4/Q17.dart",  // Specify the entry point of your Dart program relative to the workspace folder
        "args": [],                  // Optional: Command line arguments for your program
        "cwd": "${workspaceFolder}",  // Optional: Current working directory
        "preLaunchTask": "dart build", // Optional: Build task to run before launching the program
        "stopOnEntry": false,
        "sourceMapPathOverrides": {
          "file:///mnt/": "file://${workspaceFolder}/"
        }
      }
    ]
  }
  